# ==========================================
# Variables de Entorno para Tests
# ==========================================
# Usamos PostgreSQL para tests (base de datos separada)
# IMPORTANTE: DATABASE_URL debe apuntar a una base de datos PostgreSQL
# La base de datos debe existir antes de ejecutar los tests

DATABASE_URL="postgresql://neondb_owner:npg_mdARGjW92kVh@ep-round-fog-aifiqed8-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

BETTER_AUTH_SECRET=test-secret-key-for-testing-only-32chars
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# API keys de prueba (mockeadas en tests si es necesario)
GOOGLE_GENERATIVE_AI_API_KEY=test-api-key-for-unit-tests
RESEND_API_KEY=test-resend-key-for-unit-tests
EMAIL_FROM=test@noreply.local
EMAIL_FROM_NAME=MedRound Test

# Redis test (opcional, puede mockearse)
UPSTASH_REDIS_REST_URL=https://test.upstash.io
UPSTASH_REDIS_REST_TOKEN=test-token
